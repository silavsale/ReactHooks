import React, { Component } from 'react';

const todo = props => {
    return <React.Fragment>
        <imput type='text' placeholder="Todo" />
        <button type='button'>Add</button>
        <ul />
    </React.Fragment>
}